# Benchmarks

This folder contains benchmark scenarios, scripts, and generated reports.

## Run locally
```bash
./benchmarks/scripts/run_local_benchmarks.sh
```

Results are written to `benchmarks/results/`.

## Expected Inputs
- Tool under test available in `PATH`.
- Representative repositories cloned locally.

